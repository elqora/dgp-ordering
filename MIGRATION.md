# Ordering migration evidence

DGP Ordering owns customer interaction over valid, published canonical v1 definitions. This ledger maps the accepted legacy ordering outcomes to their headless destination. `Verified` means the named outcome has direct canonical replacement evidence; a green repository command alone is not migration evidence.

| Behavior slice | Legacy source and tests | Canonical destination/treatment | Status | Replacement evidence | Remaining work / approval |
| --- | --- | --- | --- | --- | --- |
| Fresh defaults and snapshot hydration | `order-flow-provider.tsx`; `use-order-flow.mode-aware-options.spec.tsx`; Instagram initialize/rehydrate suite | Session applies `default_value` only when field-id state is absent, synchronizes selectable defaults, and rehydrates canonical snapshot form names and selections without reapplying fresh defaults. | Verified | Default, hydrated-value, selectable-default, and canonical snapshot round-trip cases in `tests/migration-parity.test.ts` | Legacy camel-case snapshot shape excluded by approved clean break; outcome preserved |
| Neutral store and bindings | Legacy Form Palette-backed order-flow hooks | Framework-neutral store, field binding, session binding, registry, and component-adapter contracts; session-bound setters always reconcile. | Verified | Store subscriptions in `tests/ordering.test.ts`; session-bound effect reconciliation and registry variant/default resolution in `tests/migration-parity.test.ts` | Concrete Form Palette lifecycle remains adapter-owned |
| Selection normalization | `build-order-snapshot/selection.ts`; mode-aware option and form-selection suites | Deterministic dedupe, actual recursive ownership, visible-option filtering, canonical `multiple`, prod latest-only behavior, dev inspection behavior, and flat trigger-to-field derivation. | Verified | Prod/dev, recursive, explicit-trigger derivation, registry cardinality, and snapshot selection cases across both Ordering suites | Legacy `meta.multi` replaced by ratified `multiple`; no behavior retired |
| Hidden and stale state | option-effect snapshot cases; mode-aware visible-option suite | Fixed-point selection resolution rejects hidden fields/options without allowing stale state to bootstrap itself; newly revealed valid selections may join later iterations. | Verified | Hidden self-bootstrap, hidden field service fallback, option-effect pruning, and recursive visible-selection cases | Internal fixed-point algorithm is an implementation improvement |
| Runtime value effects | mode-aware value-effect suite | Active filter, button, and recursive-option triggers; visible targets only; option selection synchronization; ordered/chained effects; `if_empty`; user-owned overrides; `clear_on_deactivate`. Ancestor filters do not implicitly fire. | Verified | Filter, button, option, hidden-target, chained selection, precedence, clearing, and user-override cases in both suites | Canonical snake_case effect keys replace legacy spellings |
| Quantity resolution | `build-order-snapshot/quantity.ts`; complete quantity suite | First visible field rule, field-local value/values, value/length/eval, numeric coercion, multiply, clamp, fallback, then selected option, selected button field, active filter, and positive host default with provenance. The first authored field rule blocks later field rules even when it cannot resolve. | Verified | Value/length/array/eval, first-rule, transform/fallback, all default sources, non-positive handling, and provenance cases; portable expression suite | Ratified structured expression failures intentionally replace legacy silent expression fallback |
| Customer-entered validation | `evalute-field-validation.ts`; order-flow validation paths | Visible fields only; required state uses normalized selections; all twelve operators; value/length/eval subjects; regex flags; JSON structural equality; custom codes/messages; first issue per field. | Verified | Operator table plus required/hidden, length/eval, field-local arguments, JSON equality, custom output, and failure cases | Publication-rule diagnostics remain Validation-owned and are not customer output |
| Browser JavaScript expressions | legacy quantity and customer validation executors | Injectable executor plus trusted default browser executor with Spec-owned `value`/`values`, JSON-result checks, finite quantity results, and three structured host-configuration failures that prevent snapshots. | Verified | Every case in installed Spec `browser-javascript-expression-execution.json`, repeated-reference/cycle tests, and no-snapshot failure tests | Structured failure redesign was explicitly approved and ratified |
| Service evidence | `build-order-snapshot/services.ts`; service-composition suite | Visible selected base fields/options override filter default; utility nodes never become services; canonical string identity dedupes; highest known advisory rate is first; remaining services retain selection order; per-node origins remain explicit. | Verified | Filter default, highest-rate ordering, string/numeric identity, duplicate origins, hidden options, recursive options, and utility-with-service guard cases | SDK remains authoritative for final rates, prices, charges, and fulfillment |
| Quantity bounds and fallbacks | `min-max.ts`, `fallbacks.ts`; snapshot and fallback suites | Snapshot records broadest selected catalog bounds and canonical authored fallback evidence. Publication eligibility belongs to Validation/Core; handler runtime choice remains SDK-owned. | Verified | Multi-service min/max, node/global fallback preservation, null fallback, and schema-conformance cases | Legacy client-side policy pruning is replaced by the approved authority split, not silently omitted |
| Advisory utilities | `build-order-snapshot/utilities.ts`; utility snapshot cases | Visible field and selected recursive-option utilities; inherited pricing roles; flat, per-quantity, per-value, and percent calculations; parent field value; labels; all three percent bases; exact inputs/results in authored field/selection order. | Verified | Field/option cases for every mode, default/length value resolution, service guard, `base_service`, `service_total`, `all`, and authored-order accumulation | Values remain advisory evidence; SDK recalculates authoritative charges |
| Canonical snapshot construction | complete `build-order-snapshot/**` suites | Visible fields and inputs, normalized selections, quantity/provenance, bounds, service origins, fallbacks, utilities, product/schema identity, opaque meta, deterministic timestamp/configuration failures, and no editorial diagnostics. | Verified | Complete representative snapshot validates against installed Spec JSON Schema; invalid timestamp/filter/default/meta/advisory input and expression failures return no snapshot | Camel-case aliases, warnings, reserved legacy context meta, and frontend pricing policy excluded by approved v1 clean break |

No proven Ordering behavior is classified as retired. React and Form Palette rendering are assigned to `dgp-ordering-form-palette`; editorial diagnostics and publication coherence remain assigned to Validation/Workspace/Studio. The legacy engine remains evidence for observable customer outcomes, not for old wire keys, package coupling, or frontend pricing authority.
