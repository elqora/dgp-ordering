# Ordering migration evidence

DGP Ordering preserves proven customer-order behavior through framework-neutral canonical v1 contracts. Removing frontend pricing authority and Form Palette coupling does not permit reducing ordering behavior.

| Legacy behavior | Legacy evidence | Destination | Preserved outcome | Implementation status | Tests/evidence | Explicit approval |
| --- | --- | --- | --- | --- | --- | --- |
| Default and visible customer state | `src/utils/build-order-snapshot/inputs.ts`, ordering hooks and tests | DGP Ordering | Preserve canonical default-value hydration and visible-field input collection while excluding hidden stale values from snapshots. | Visible collection implemented; neutral default hydration pending migration | Current snapshot tests; legacy initialization/default tests pending | Not required—preserve |
| Selection normalization | selection and field-input hooks/tests | DGP Ordering | Preserve deterministic traversal, cardinality, unavailable-option rejection, recursive ownership, and context reconciliation. | Implemented; exhaustive contextual parity audit pending | Current selection tests; legacy mode-aware and recursive cases pending | Not required—preserve |
| Hidden stale state | visibility and selection hooks/tests | DGP Ordering | Preserve rejection of selections owned by unreachable fields without allowing self-bootstrap. A fixed-point algorithm is an internal improvement only. | Implemented; oscillation and combined-effect cases pending | Current stale-selection test; legacy visibility matrix pending | Not required—preserve |
| Quantity resolution | `build-order-snapshot/quantity.ts` and tests | DGP Ordering | Preserve field rule, selected option, selected field, active filter, and host-default precedence, transforms, bounds, and provenance. | Implemented; full legacy matrix pending | Current quantity test; legacy quantity suite pending | Not required—preserve |
| Customer validation | legacy field-validation functions and tests | DGP Ordering | Preserve visible-field required and operator behavior, custom messages, value transforms, and expression results. | Partially implemented; full operator and input-shape matrix pending | Current required-field test; legacy validation cases pending | Not required—preserve |
| Browser JavaScript expressions | quantity and field expression paths/tests | Spec and DGP Ordering | Preserve successful function-body semantics and precisely ratify `value`/`values` arguments; structured failures prevent snapshots. | Executor implemented; argument-semantics reconciliation pending migration | Current failure tests; legacy successful-expression cases pending | Structured failures approved; any argument redesign requires explicit approval |
| Service evidence and selection | snapshot services, selection, fallback tests | DGP Ordering with Core | Preserve selected-node origins, deterministic authored order, filter defaults, service maps, and fallback evidence without claiming final pricing authority. | Partially implemented; full service/fallback parity pending | Current service snapshot test; legacy service-map suite pending | SDK pricing authority and package split approved |
| Advisory utilities | legacy utility builders and snapshot tests | DGP Ordering | Preserve field- and option-level flat, per-quantity, per-value, and percent results and exact snapshot evidence. | Option-level path implemented; ordinary field-level utility migration pending | Current option-utility test; legacy utility suite pending | Advisory-only contract approved; behavior preserved |
| Snapshot construction | `build-order-snapshot/**` and tests | DGP Ordering and Spec | Preserve complete canonical customer evidence, bounds, selections, inputs, quantity source, services, fallbacks, utilities, metadata, and deterministic failure behavior. | Partially implemented; exhaustive parity and conformance matrix pending | Current snapshot tests and Spec fixture; legacy snapshot suites pending | Canonical wire redesign approved; behavioral evidence preserved |
| Form integration | direct Form Palette hooks/components | DGP Ordering Form Palette | Preserve the useful form lifecycle and Wrapper experience through neutral Ordering contracts and the optional adapter. | Neutral contracts implemented; adapter parity pending | Adapter migration record | Workspace dependency constitution |
| Editorial diagnostics | legacy order-flow/editor overlap | DGP Validation and Studio | Preserve publication diagnostics in their destination while keeping them out of customer-facing ordering responses. | Partial in destination packages | Validation and Studio migration records | Workspace package-boundary decision |

Legacy snapshot aliases, camel-case wire keys, deprecated fields, and direct React/Form Palette dependencies are excluded by the approved representation clean break. Proven ordering outcomes remain required. Frontend rate ranking is not authoritative selection policy, but catalog/rate coherence remains a Validation responsibility.
